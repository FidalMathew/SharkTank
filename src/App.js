import NavBar from './components/navbar';
import Panel from './components/panels';
import './styles/App.css';

function App() {
  return (
    <div className="App">
      {/* Don't add the codes directly here. Make a component and add it here :) */}
      <NavBar />
      <Panel />
    </div>
  );
}

export default App;
